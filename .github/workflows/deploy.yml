name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Build project
      run: npm run build --if-present

    - name: Build Docker image
      run: docker build -t vaibhavbg/emileage .

    - name: Push Docker image to Docker Hub
      run: |
        echo ${{ secrets.P4jkbnixj4@ }} | docker login -u ${{ secrets.vaibhavbg }} --password-stdin
        docker push your-dockerhub-vaibhavbg/emileage
